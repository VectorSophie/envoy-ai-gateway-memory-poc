apiVersion: aigateway.envoyproxy.io/v1alpha1
kind: AIGatewayRoute
metadata:
  name: ai-route-openai
  namespace: default
spec:
  parentRefs:
    - name: ai-gateway
      kind: Gateway
      group: gateway.networking.k8s.io
  rules:
    - backendRefs:
        - name: ai-service-openrouter-external
          # 운영 전환/리허설 시에만 apply한다.
          # 기본 PoC 경로는 계속 aigateway-route.yaml(mock) 기준이다.
